{
  id: board.id,
  title: board.title,
  description: board.description,
  updated_at: board.updated_at,
  created_by: {
    id: board.user.id,
    name: board.user.name
  },
  board_posts: board.board_posts
}